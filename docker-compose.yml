services:
  flask:
    build: ./backend
    ports:
      - "5000:5000"
    env_file:
      - .env

  vue:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    stdin_open: true
    tty: true

